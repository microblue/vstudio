# =============================================================================
# VStudio 环境变量清单
# 复制为 .env 后填入实际值
# 详见 doc/06-tech-stack.md § 环境变量说明
# =============================================================================

# ── Supabase ──────────────────────────────────────────────────────────────────
# 项目 URL（Dashboard → Settings → API）
PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# 匿名 Key（前端用，受 RLS 限制）
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJI...
# Service Role Key（仅 Edge Function / 迁移脚本使用，切勿暴露到前端）
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJI...

# ── Anthropic (LLM) ──────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── OpenAI（可选，多模型支持时需要）────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ── DeepSeek（可选）────────────────────────────────────────────────────────
DEEPSEEK_API_KEY=sk-...

# ── Replicate（图片/视频生成）─────────────────────────────────────────────
REPLICATE_API_TOKEN=r8_...

# ── fal.ai（图片/视频生成，备选）──────────────────────────────────────────
FAL_KEY=key_...

# ── Fish Audio (TTS) ─────────────────────────────────────────────────────────
FISH_AUDIO_API_KEY=...

# ── FFmpeg 合成服务（自建）────────────────────────────────────────────────
FFMPEG_SERVICE_URL=http://localhost:3100
FFMPEG_SERVICE_SECRET=your-shared-secret

# ── Webhook ──────────────────────────────────────────────────────────────────
# 用于第三方 API 回调的公开 URL（通常 = PUBLIC_SUPABASE_URL）
WEBHOOK_BASE_URL=https://YOUR_PROJECT.supabase.co/functions/v1

# ── 开发环境 ─────────────────────────────────────────────────────────────────
# Supabase CLI 本地开发
SUPABASE_DB_URL=postgresql://postgres:postgres@localhost:54322/postgres
